// Low-level group primitives removed: use PackGroupSession::create_sender()
// and PackGroupSession::from_distribution() through the high-level API.
// SenderKeyRecord remains public for store trait implementations only.
